To execute the input checker the following steps are necessary:

1) The program that should be analysed must be put inside the example/ folder
2) The input file that should be checked must be inside the example/ folder
3) To start the input checker (and static analysis) run data_quality_controller.py