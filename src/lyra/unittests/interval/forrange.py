# INITIAL: i -> [-inf, inf], b -> [-inf, inf]
for i in range(10):
    # STATE: i -> [0, 9], b -> [-inf, inf]
    b: int = 1
    # STATE: i -> [0, 9], b -> [1, 1]
# FINAL: i -> [-inf, inf], b -> [-inf, inf]
